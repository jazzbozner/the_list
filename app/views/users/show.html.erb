<h1> Hello <%= @user.username %></h1>
<%= button_to "All Lists", lists_path, method: :get %><br>
<%= button_to "Log out", logout_path, method: :delete %>
 <ul>Lists:
    <% @user.lists.each do |list| %>
         <li><%= link_to list.name, list %> - <%= list.description %></li><br>
    <% end %>
    </ul>
